{
  "name": "database",
  "version": "1.0.0",
  "scripts": {
    "start": "sls dynamodb start --migrate --stage dev",
    "install": "sls dynamodb install",
    "deploy": "sls deploy --aws-profile devprofile --stage dev"
  }
}
